import tensorflow as tf
import pandas as pd
import numpy as np
import tensorflow as tf
# from sklearn.linear_model import LassoCV

#Create DataFrames for attribute and target matrices
num_features = 3
num_samples = 400
num_regions = 
random_W = np.random.rand(num_features, 1)
random_x = np.random.rand(num_samples, num_features)
